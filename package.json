{
  "name": "@shynome/thrift-server",
  "version": "0.0.1",
  "publishConfig": {
    "access": "public"
  },
  "description": "为 webserver handler 添加一个 context 参数, 方便注入认证相关的信息",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "jest",
    "preversion": "npm t",
    "postversion": "git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shynome/thrift-server.git"
  },
  "keywords": [
    "apache",
    "thrift"
  ],
  "author": "shynome",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/shynome/thrift-server/issues"
  },
  "homepage": "https://github.com/shynome/thrift-server#readme",
  "peerDependencies": {
    "thrift": "^0.12.0"
  },
  "devDependencies": {
    "@shynome/thrift-typescript": "^3.8.1",
    "@types/jest": "^24.0.18",
    "@types/thrift": "^0.10.8",
    "jest": "^24.9.0",
    "thrift": "^0.12.0",
    "ts-jest": "^24.1.0"
  }
}
